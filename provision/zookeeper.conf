#!upstart
description "Zookeeper Server"

start on filesystem and net-device-up IFACE!=lo
stop on shutdown

exec /opt/kafka/bin/zookeeper-server-start.sh /opt/kafka/config/zookeeper.properties >> /var/log/zookeeper/zookeeper.log 2>&1
